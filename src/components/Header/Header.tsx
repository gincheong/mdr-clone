import styled from 'styled-components';

const StyledHeader = styled.header`
  padding: 20px;
`;

const Header = () => {
  return (
    <StyledHeader>
      this is Header
    </StyledHeader>
  )
};

export default Header;
